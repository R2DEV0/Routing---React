import React from 'react';
import { Link } from '@reach/router';

const Home = (props) => {

    return (
        <div>
            <div> <h1>Welcome to the Home Page</h1> </div>
        </div>
    );
}

export default Home;